# This file specifies files that are *not* uploaded to Google Cloud
# using gcloud. It follows the same syntax as .gitignore, with the addition of
# "#!include" directives (which insert the entries of the given .gitignore-style
# file at that point).
#
# For more information, run:
#   $ gcloud topic gcloudignore
#

# Exclude common development files and directories
.git
.gitignore
__pycache__/
*.pyc

# Exclude Node.js related files (for the frontend)
node_modules/
package-lock.json
yarn.lock

# Exclude development and build files for the frontend
/frontend/node_modules/
/frontend/build/
/frontend/.cache/
/frontend/.npm/
/frontend/.env
/frontend/.env.local
/frontend/.env.development
/frontend/.env.test
/frontend/.env.production
/frontend/.env.*.local
/frontend/.env.*.development
/frontend/.env.*.test
/frontend/.env.*.production

# Exclude other files generated by the development process (e.g., logs)
/frontend/npm-debug.log*
/frontend/yarn-debug.log*
/frontend/yarn-error.log*
/frontend/.yarn-integrity

# Exclude editor and IDE configuration files
.vscode/
.idea/
*.iml
*.ipr
*.iws

# Exclude Docker-related files (if not using Docker)
Dockerfile
docker-compose.yml
